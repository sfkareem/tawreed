"""Page widgets for the MainWindow's QStackedWidget.

Each module in this package exposes a single QWidget subclass named
``*Page`` (e.g. ``WorkspacePage``). They are pure presentation; all
business logic lives in ``core/``.
"""
